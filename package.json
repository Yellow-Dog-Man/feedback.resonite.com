{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "previewX": "npm run build && vite preview",
    "preview": "npm run build && npx wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.54.8",
    "vite": "^8.2.2",
    "wrangler": "^4.131.1"
  },
  "dependencies": {
    "formsmd": "^1.1.3",
    "hono": "^4.7.2"
  }
}
